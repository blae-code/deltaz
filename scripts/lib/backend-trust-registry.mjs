export const backendTrustRegistry = {
  abortMissionPlan: { lane: 'canonical', trigger: 'frontend', allowsLLM: false, allowsRandom: false, classification: 'keep' },
  acceptMissionPlan: { lane: 'canonical', trigger: 'frontend', allowsLLM: false, allowsRandom: false, classification: 'keep' },
  activateMissionPlan: { lane: 'canonical', trigger: 'frontend', allowsLLM: false, allowsRandom: false, classification: 'keep' },
  aiNarrator: { lane: 'advisory', trigger: 'manual', allowsLLM: true, allowsRandom: false, classification: 'advisory_only' },
  colonyAlertMonitor: { lane: 'deterministic_projection', trigger: 'automation', allowsLLM: false, allowsRandom: false, classification: 'keep' },
  commodityPriceEngine: { lane: 'canonical', trigger: 'automation', allowsLLM: false, allowsRandom: false, classification: 'keep' },
  completeMissionPlan: { lane: 'canonical', trigger: 'frontend', allowsLLM: false, allowsRandom: false, classification: 'keep' },
  economyCycle: { lane: 'canonical', trigger: 'automation', allowsLLM: false, allowsRandom: false, classification: 'keep' },
  factionSimulation: { lane: 'canonical', trigger: 'automation', allowsLLM: false, allowsRandom: false, classification: 'deterministic_rewrite' },
  failMissionPlan: { lane: 'canonical', trigger: 'frontend', allowsLLM: false, allowsRandom: false, classification: 'keep' },
  finalizeOrigin: { lane: 'canonical', trigger: 'frontend', allowsLLM: false, allowsRandom: false, classification: 'deterministic_rewrite' },
  generateJournalEvent: { lane: 'canonical', trigger: 'frontend', allowsLLM: false, allowsRandom: false, classification: 'deterministic_rewrite' },
  generateMission: { lane: 'canonical', trigger: 'frontend', allowsLLM: false, allowsRandom: false, classification: 'deterministic_rewrite' },
  generateMissionPlan: { lane: 'canonical', trigger: 'frontend', allowsLLM: false, allowsRandom: false, classification: 'keep' },
  listServerFiles: { lane: 'canonical', trigger: 'admin', allowsLLM: false, allowsRandom: false, classification: 'keep' },
  missionForge: { lane: 'canonical', trigger: 'admin', allowsLLM: false, allowsRandom: false, classification: 'deterministic_rewrite' },
  missionOps: { lane: 'canonical', trigger: 'frontend', allowsLLM: false, allowsRandom: false, classification: 'deterministic_rewrite' },
  notifyMissionAndThreats: { lane: 'deterministic_projection', trigger: 'automation', allowsLLM: false, allowsRandom: false, classification: 'keep' },
  operativeAnalytics: { lane: 'advisory', trigger: 'frontend', allowsLLM: true, allowsRandom: false, classification: 'advisory_only' },
  processTrades: { lane: 'canonical', trigger: 'admin', allowsLLM: false, allowsRandom: false, classification: 'keep' },
  resolveJournalChoice: { lane: 'canonical', trigger: 'frontend', allowsLLM: false, allowsRandom: false, classification: 'deterministic_rewrite' },
  riskAssessment: { lane: 'canonical', trigger: 'frontend', allowsLLM: false, allowsRandom: false, classification: 'keep' },
  scavengeRun: { lane: 'canonical', trigger: 'frontend', allowsLLM: false, allowsRandom: false, classification: 'deterministic_rewrite' },
  serverManager: { lane: 'canonical', trigger: 'admin', allowsLLM: false, allowsRandom: false, classification: 'keep' },
  settlementSim: { lane: 'canonical', trigger: 'frontend', allowsLLM: false, allowsRandom: false, classification: 'deterministic_rewrite' },
  steamAuth: { lane: 'canonical', trigger: 'frontend', allowsLLM: false, allowsRandom: false, classification: 'keep' },
  survivorEngine: { lane: 'canonical', trigger: 'admin', allowsLLM: false, allowsRandom: false, classification: 'deterministic_rewrite' },
  tacticalAdvisor: { lane: 'advisory', trigger: 'frontend', allowsLLM: true, allowsRandom: false, classification: 'advisory_only' },
  threatAnalysis: { lane: 'advisory', trigger: 'frontend', allowsLLM: true, allowsRandom: false, classification: 'advisory_only' },
  tradeRequestOps: { lane: 'canonical', trigger: 'frontend', allowsLLM: false, allowsRandom: false, classification: 'keep' },
  treatyEngine: { lane: 'canonical', trigger: 'frontend', allowsLLM: false, allowsRandom: false, classification: 'keep' },
  weeklyDossier: { lane: 'advisory', trigger: 'frontend', allowsLLM: false, allowsRandom: false, classification: 'advisory_only' },
  whitelistPlayer: { lane: 'canonical', trigger: 'admin', allowsLLM: false, allowsRandom: false, classification: 'keep' },
  worldPulse: { lane: 'deterministic_projection', trigger: 'automation', allowsLLM: false, allowsRandom: false, classification: 'deterministic_rewrite' },
};

export const canonicalTrustLanes = new Set(['canonical', 'deterministic_projection']);
